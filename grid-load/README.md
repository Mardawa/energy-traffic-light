# Energy Traffic lights

## Quickstart

Streamlit app

```bash
cd grid-load
poetry install --sync
poetry run streamlit run streamlit.py
```

FastAPI app

```bash
cd grid-load
poetry install --sync
poetry run fastapi dev main.py
```
